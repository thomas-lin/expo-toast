package com.maxwin.modules.toast;

import android.content.Context;
import android.view.View;

import org.unimodules.core.ModuleRegistry;

public class UnimoduleToastView extends View {
  public UnimoduleToastView(Context context, ModuleRegistry moduleRegistry) {
    super(context);
  }
}
